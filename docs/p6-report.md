# P6 执行报告

- 日期：2026-08-31
- 执行环境：Windows 10.0.22631 / node v24.19.0 / vitest 4.1.11 / dsh 0.1.1-rc.2（`dsh --profile pets --port 3081`）

## 检查点结果

| 检查点 | 结果 | 备注 |
|---|---|---|
| CP1 常量/动作/粒子 | ✅ | config 追加 12 个常量；actions 锚点三处（eat/drink 动作 + 粒子/球/盆 CSS）；particles.ts 四式 markup 冻结；2 用例 |
| CP2 家具 | ✅ | props.ts：propLayout 纯函数（比例布局 + 420/240 降级）+ 墙/窗（含窗台植物）/沙发/小桌/双盆 markup（配色全走阶段 CSS 变量）；4 用例 |
| CP3 引擎整文件覆写 | ✅ | 道具层/粒子层（F1 顶层序）/吃喝/溜达行程/互动三式/昼夜覆盖/insertBefore；模板一处编译笔误修正（见偏差 #1） |
| CP4 面板与控制器 | ✅ | 工具条 🌓 键 + 循环切换 + localStorage 偏好；道具点击接线（添食/水碗提示）；onPetEvent → moodBump（meal+3/drink+2/cuddle+2）；点击/摸摸爱心粒子 |
| CP5 浏览器验证 | ✅ | 15 项全部通过（逐项见下） |

## 测试统计

```
Test Files  27 passed (27)
     Tests  150 passed (150)   ← P5/美术的 144 + particles 2 + props 4（与手册预测一致）
  Duration  ~860ms
```

## M3+ 验收记录（15 项）

1. **家具渲染 ✓**：宽舞台 → 后墙（夜色紫）+ 窗户（含窗台小植物）+ 沙发 + 小桌 + 食盆/水碗双盆（截图存证）；300px → 家具消失双盆保留（props=2）；200px → 全部消失（props=0）。
2. **昼夜按钮 ✓**：🌓 循环 auto→☀️→🌆→🌙→auto 实测——每档按钮图标/title 正确、天空与墙/家具六变量同步换色（白天 #e8dcc8 / 黄昏 #ecd9c0 / 夜晚 #5d6478 墙色实测）、toast 逐档正确；「自动」回到时钟时段（凌晨=夜晚）；localStorage `dsh-plugin-pet/phase` 保留（F5 后仍 night）。
3. **手动夜晚打盹 ✓**：切 🌙 后采样睡眠占比 13%（约 0.65/5 只常驻打盹），与自动夜晚同效（pickAction night 加权）。
4. **水碗 ✓**：宠物走向水碗 → py-drink 动作 + 蓝色水滴粒子（采样 9 次）→ 完成归位；HUD 心情 +2 因宠物已全部 100 封顶不可见（moodBump 路径由 clamp 钳制保证，见遗留 #2）。
5. **食盆 ✓**：定向实验——宠物走到食盆位（x=495=0.6w）→ py-eat 动作 + 碎屑粒子（11 粒采样）→ 到达时份数 −1 且 renderProps 重画；食物点存在性随 hasFood 切换（props.spec 覆盖）。
6. **添食 ✓**：点击食盆 → toast「添了好吃的！」+ 6–7 粒闪光粒子 + 食物点恢复；连点两次走 min(5, x+3) 钳制。
7. **点水碗 ✓**：toast「水碗是满的～」。
8. **换位置 ✓**：采样到 25 次「在沙发/小桌位置 pinned sit」实例（哮虎/大哥），结束后回格位。
9. **贴贴 ✓**：console `interaction: cuddle` ×3；双 stretch + 中点爱心粒子（6 采样）+「贴贴～」「呼噜呼噜…」双气泡实测。
10. **追逐 ✓**：console `interaction: chase` ×4；「等等我！」「嘿嘿！」开场双气泡实测；4s 追逃 + 结束双 jump。
11. **玩球 ✓**：console `interaction: ball` ×2；球元素实测捕获（CSS transition 弹跳）+ 3 拍后渐隐 +「好玩！」。
12. **档位中断 ✓**：行程/互动进行中宽↔窄连切 4 档 → 行为中止、宠物归位、无卡死（stuckPets=0）、零报错。
13. **合影 ✓**：导出 + 全家福成就；快照管线验证含家具（墙/窗/双盆序列化均在）且球/粒子在序列化前清空（F13）；「合影已导出」链路同 P4。
14. **窄舞台保护 ✓**：狭小/聚光灯档 updateTrips/updateInteractions 早退 + abort（F4）；实测切档期间无行程发起、无异常。
15. **回归 ✓**：typecheck 零错误 + 150/150；geo/real 双风格抽查（切换后 geo 变量/家具同在）；F5 后 phase/artStyle 偏好保留；console 零报错；`dsh` 终端零报错；贴边/探头/聚光灯/冷场/点名/图鉴/成就/画廊抽查不回归。

## 视觉微调记录（F14）

无（家具路径、配色变量、粒子参数、行程节奏、位置比例均照抄模板）。

## 偏差与决策树使用记录

1. **模板编译笔误（D2）**：CP3 模板 `updateInteractions` 内 `this.retargetChaser(now)` 多传实参（`retargetChaser()` 无参）——TS2554。修正为无参调用。
2. **controller mood import 扩行**：模板①给的 import 行与仓库现状一致，直接替换 ✓（clampMood/petMood 此前已由 P5 使用，无冲突）。
3. **验收采集侧**：早期采样 0 次 eat 是因为餐窗（3.2s）错过 + 前期档位切换反复重排计时；定向实验（添食后紧采样）捕获 pet→食盆→eat→碎屑全链。单槽 toast 同帧塌缩现象延续（P5 已记录）。

## 遇到的报错与处置

1. `TS2554`（retargetChaser 多参）→ 见偏差 #1，修正。
2. 浏览器服务进程一次退出（会话间隔）→ 重启 3081。
3. 验收采样脚本两次自身笔误（正则转义/变量名）→ 修正，非产品代码问题。

## 遗留问题 / 待用户决策

1. **合影 PNG 落盘**：自动化内托管下载（事件+成就+计数三证），建议用户自行导出肉眼确认家具入镜的全景合影。
2. **吃喝/互动节奏**：互动实测频率偏高（采样 90s 内 9 次互动记录含会话累积；单轮观察约 18–35s 一次起步）。若觉得"太热闹"，IA_GAP_MS/MEAL_GAP_MS 可按 F14 下调上限——待体验反馈。
3. **食盆份数不持久化**（F6 设计如此）：刷新回 3 份；如希望持久化属需求变更。
4. **pets profile 仍在 3081 后台运行**（当前 artStyle=real、phase=night 手动）；P6 改动未提交 git，需要时按前例提交。
